# Package

version     = "0.1.0"
author      = "endes"
description = "CNN for reading lips phonemes on a video"
license     = "MIT"
srcDir      = "src"

# Deps

requires "arraymancervision >= 0.1.0"
requires "arraymancervision >= 0.0.3"
requires "arraymancer >= 0.5.0"
requires "cligen >= 0.9.19"
requires "nim >= 0.19.0"